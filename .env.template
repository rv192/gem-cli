# ---------------------------------------------------------------------------
# OpenAI-兼容 API 配置
#
# 如果您使用兼容OpenAI规范的API（例如本地部署的LLM或第三方提供的服务），请在此处配置。
# ---------------------------------------------------------------------------

# [必需] API 的基础 URL。
# 例如，对于本地模型，它可能是 "http://localhost:8080"。
# 对于云服务，它将是提供商提供的特定端点。
OPENAI_BASE_URL=https://gemini.72live.com

# [必需] 用于访问服务的 API 密钥。
# 请从您的服务提供商处获取此密钥。
OPENAI_API_KEY=

# ---------------------------------------------------------------------------
# 模型配置
#
# 在这里指定您希望应用程序使用的默认和备用模型。
# ---------------------------------------------------------------------------

# [可选] 默认使用的主要模型名称。
# 如果未指定，将使用服务提供的默认模型。
# 例如: gemini-1.5-pro-latest
DEFAULT_MODEL=gemini-2.5-pro

# [可选] 备用模型列表，以逗号分隔。
# 当主要模型 (DEFAULT_MODEL) 不可用时，应用程序将按顺序尝试使用这些模型。
# 例如: gemini-1.5-flash,gemini-1.0-pro
FALLBACK_MODELS=gemini-2.5-flash,gemini-2.5-lite,gemini-2.0-flash

# [可选] 用于文本嵌入的默认模型。
# 如果您的应用需要生成文本嵌入（例如用于RAG或语义搜索），请在此处指定模型。
# 例如: text-embedding-ada-002
DEFAULT_EMBEDDING_MODEL=Qwen/Qwen3-Embedding-8B

# ---------------------------------------------------------------------------
# 旧版或特定服务支持
#
# 此部分用于向后兼容或特定服务的配置。
# ---------------------------------------------------------------------------

# [可选] SiliconFlow 服务的特定 API 密钥。
# 仅在 OPENAI_API_KEY 未设置时，此密钥和对应的 SiliconFlow 服务才会被用作备用选项。
SILICONFLOW_API_KEY=sk-ybhnlsuxeobtrbijnowwrvloegnguaihmjvervuhqqzrhzqm

# [可选] SiliconFlow 服务的默认模型。
# 如果未设置，将使用 THUDM/GLM-4-9B-0414 作为默认模型。
SILICONFLOW_DEFAULT_MODEL=THUDM/GLM-4-9B-0414